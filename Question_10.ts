

// This program is written by Bing on 01 Sep 2023
// It prints "Hello, world!" to the console

console.log("Hello, world!");

// This program is written by Bing on 01 Sep 2023
// It calculates the area of a circle given its radius

const PI: number = 3.14;

// Declare a function called areaOfCircle that takes a parameter called radius
function areaOfCircle(radius: number): number {
  // Return the value of PI times radius squared
  return PI * radius * radius;
}

// Test the function with some examples
console.log(areaOfCircle(1)); // Should print 3.14
console.log(areaOfCircle(2)); // Should print 12.56
console.log(areaOfCircle(3)); // Should print 28.26
